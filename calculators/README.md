# Calculator Pages

Each calculator will become a standalone, browser-based page under this directory:

- `supplier-quote-comparison/`
- `landed-cost/`
- `incoterm-cost-comparison/`
- `maximum-purchase-price/`
- `cbm-container/`

The pages are intentionally reserved for the next implementation phase. V1 keeps the homepage navigation ready without adding calculation logic yet.